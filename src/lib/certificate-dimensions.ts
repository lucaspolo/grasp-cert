/**
 * Dimensões do certificado, em módulo próprio para que o editor as importe sem
 * arrastar next/og, qrcode e fs para o bundle do cliente.
 */
export const CERTIFICATE_WIDTH = 800;
export const CERTIFICATE_HEIGHT = 500;
