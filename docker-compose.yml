services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: grasp
      POSTGRES_PASSWORD: grasp
      POSTGRES_DB: grasp_cert
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
